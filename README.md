### Running with docker: 


Building images:
```bash
docker-compose build

```


Starting up with docker:
```bash
docker-compose up -d
```

Navigate to http://localhost:8080/



Stop and remove containers (db volume will not be removed, unless explicitly specified):
```
docker-compose down
```
